package com.app.factory;
/**
 * To add any new kind of chart to be supported other than BAR and LINE, a new type should be added
 * A new class should be created to build the requested chart dataset
 * */
public enum ChartType {
    BAR,
    LINE
}
